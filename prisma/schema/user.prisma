model User{
    id String @id @default(cuid())
    name String
    email String
    password String

    participants Participant[]
    messages Message[]

    createdAt DateTime @default(now())    
}